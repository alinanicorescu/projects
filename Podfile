# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'test.orange' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Alamofire'
  pod 'Charts'
  pod 'TinyConstraints'
  # Pods for test.orange

  target 'test.orangeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'test.orangeUITests' do
    # Pods for testing
  end

  
end
